[package]
name = "versatiles_glyphs"
version = "0.9.1"
edition = "2021"
rust-version = "1.85"
description = "A tool for generating SDF glyphs from fonts."
license = "Unlicense"
repository = "https://github.com/versatiles-org/versatiles-glyphs-rs"
documentation = "https://docs.rs/versatiles_glyphs"
readme = "README.md"
keywords = ["font", "glyph", "sdf", "ttf", "maplibre"]
categories = ["graphics", "rendering", "command-line-utilities"]
exclude = [
	".github",
	".gitignore",
	"codecov.yml",
	"pages",
	"rust-toolchain.toml",
	"rustfmt.toml",
	"scripts",
	"testdata",
]

# cargo-release settings (used by scripts/release.sh).
# The pre-release hook regenerates CHANGELOG.md so it lands in the release commit.
[package.metadata.release]
pre-release-commit-message = "chore: Release {{crate_name}} version {{version}}"
pre-release-hook = ["scripts/update_changelog.sh"]

[lib]
name = "versatiles_glyphs"
path = "src/lib.rs"

[[bin]]
name = "versatiles_glyphs"
path = "src/main.rs"
required-features = ["cli"]

[profile.dev]
opt-level = 1
debug = true
rpath = false
lto = false
debug-assertions = true
panic = "unwind"
incremental = true
overflow-checks = true

[profile.release]
opt-level = 3
debug = false
rpath = false
lto = true
debug-assertions = false
codegen-units = 1
panic = "abort"
incremental = false
overflow-checks = false
strip = true

[dependencies]
anyhow = "1.0.103"
clap = { version = "4.6.1", features = ["derive", "std"], optional = true }
indicatif = "0.18.6"
prost = "0.14.4"
rayon = "1.12.0"
regex-lite = "0.1.9"
rstar = "0.13.0"
serde = { version = "1.0.228", features = ["derive"], optional = true }
serde_json = { version = "1.0.150", optional = true }
ttf-parser = { version = "0.25.1", features = ["std"] }

[dev-dependencies]
tar = "0.4.46"
tempfile = "3.27.0"

[features]
default = ["cli"]
cli = ["dep:clap", "dep:serde_json", "dep:serde"]
